# Transform Package

The transform package provides function to transform data into various kinds of structures defined in the models package.

## Pairs structure

The primary functions (`CreateInterExchangePairs()`, and `CreateIntraExchangePairs()`) allow to transform exchanges structure into pairs structure while properly accounting for all fees, liquidity, and the side of the transaction.
